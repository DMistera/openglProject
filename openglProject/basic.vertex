#version 330 core

uniform mat4 p_matrix;
uniform mat4 v_matrix;
uniform mat4 m_matrix;

layout(location = 0) in vec3 vertexPosition_modelspace;

void main(){
	vec4 v;
	v.xyz = vertexPosition_modelspace;
	v.w = 1.0;
	gl_Position.xyz = p_matrix*v_matrix*m_matrix*v;
}